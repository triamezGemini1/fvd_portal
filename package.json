{
  "name": "fvd-portal",
  "private": true,
  "description": "Metapaquete: el front Vite está en la carpeta hermana fvd_panel (C:\\wamp64\\www\\fvd_panel).",
  "scripts": {
    "postinstall": "npm install --prefix ../fvd_panel",
    "dev": "npm --prefix ../fvd_panel run dev",
    "build": "npm --prefix ../fvd_panel run build && node scripts/copy-fvd-panel-build.cjs",
    "preview": "npm --prefix ../fvd_panel run preview"
  }
}
